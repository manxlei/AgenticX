import { describe, expect, it } from "vitest";

describe("sso-providers repository", () => {
  it("placeholder repository test", () => {
    // Repository integration requires a running PostgreSQL test database.
    // This placeholder keeps test discovery stable in local environments.
    expect(true).toBe(true);
  });
});
